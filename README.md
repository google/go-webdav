Implementation of a self-contained WebDAV server in Go. It supports most
features of RFC 4918; except for shared locks.

Verified using the WebDAV litmus tests.

This is not an official Google product.
